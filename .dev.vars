# Local-only secrets for `wrangler dev` / `opennextjs-cloudflare preview`.
# Gitignored — never commit. Production secrets live in the Cloudflare
# dashboard (Settings → Variables and Secrets) as encrypted Secrets.
NEXTAUTH_SECRET=98E3B2CC28F61492C6934531C828C
