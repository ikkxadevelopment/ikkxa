
import LoginWidget from "@/components/LoginWidget";

export default async function ProductsPage() {
    return (
        <main className="min-h-screen pt-[70px] bg-slate-100 flex items-center">
            <div className="max-w-[400px] mx-auto">
                <LoginWidget />
            </div>
        </main>
    );
}

