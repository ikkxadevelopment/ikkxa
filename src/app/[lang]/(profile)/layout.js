import AccountLayout from "@/components/AccountLayout";

export default function ProfLayout({ children }) {
    return (
        <AccountLayout>
            {children}
        </AccountLayout>
    )
}